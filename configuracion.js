/* ============================================
   configuracion.js -- aqui se conecta con el backend
   ============================================
   El backend ya esta corriendo en la compu del
   proyecto, en el puerto 8000. Aqui nomas se pone
   la url base, no hay que tocar nada mas.

   Si abres el panel en la MISMA compu del backend,
   dejalo en "localhost". Si lo abres desde otra
   compu de la red, cambia "localhost" por la ip
   real de esa maquina (ej: "http://192.168.1.50:8000")
   ============================================ */
const URL_BASE = 'http://localhost:8000';
