/* ============================================
   configuracion.js — Conexión con el backend
   ============================================
   El backend ya corre en la PC del proyecto, en el
   puerto 8000. Aquí solo se define la URL base.
   Si el navegador corre en la MISMA PC que el backend,
   deja "localhost". Si abres el panel desde otra PC
   de la red, cambia "localhost" por la IP real de esa
   máquina (ej: "http://192.168.1.50:8000").
   ============================================ */
const API_BASE = 'http://localhost:8000';
